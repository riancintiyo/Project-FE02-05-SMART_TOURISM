import React from 'react';
import Table from '../components/pagination/Table';

const Home = () => {
    return (
        <div>
            <Table />
        </div>
    );
}

export default Home;